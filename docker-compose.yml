version: "3"

services:
  python:
    container_name: python-django
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    # command: python manage.py runserver 0.0.0.0:8000
    command: python manage.py startapp timeline
    volumes:
      - ./hackathon:/usr/src/app/
